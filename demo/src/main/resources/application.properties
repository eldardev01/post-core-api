#Application config
server.port = 0
spring.application.name=post-discovery-api

#Validation config
server.error.include-message=always
server.error.include-binding-errors = always

#Eureka config
eureka.instance.instance-id=${spring.application}:${random.value}
eureka.client.service-url.defaultZone=http://localhost:8010/eureka